import fs from 'node:fs';import assert from 'node:assert/strict';
const app=fs.readFileSync(new URL('./app.js',import.meta.url),'utf8');
const css=fs.readFileSync(new URL('./styles.css',import.meta.url),'utf8');
assert.ok(app.includes('data-sync-gated="1"'),'Unsynchronized speaking source is not playback-gated');
assert.ok(app.includes("const syncGated=video.dataset.syncGated==='1'")&&app.includes("video.dataset.voiceSync='visual-only'"),'Playback binder does not identify visual-only source review mode');
assert.ok(!app.slice(app.indexOf("video.addEventListener('play'"),app.indexOf("video.addEventListener('pause'")).includes('startSceneVideoVoicePlayback'), 'Gated source preview must not fake lip-sync with detached approved audio');
assert.ok(!app.includes("return 'Preparing final clip…'"),'Overlay status text still appears on video');
assert.match(app,/sceneSyncStateUi/,'External scene sync status is missing');
assert.ok(css.includes('.scene-visual video[data-sync-gated="1"]{pointer-events:auto;cursor:pointer}'),'Gated source preview must remain clickable while muted');
assert.match(app,/sceneProductionReady\(project=\{\},scene=\{\}\).*sceneHasValidatedLipSync/s,'Production readiness no longer requires validated sync');
console.log('v1.9.81 sync-gated muted-preview interactivity regression: PASS');
